<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.mybatis.IS_cateDAO">

   <select id="list" resultType="com.test.mybatis.S_cateDTO">
		SELECT S_CATEGORY_CODE, L_CATEGORY_CODE,  S_CATEGORY_NAME
		FROM TBL_S_CATEGORY
   </select>
   
   <select id="search" resultType="com.test.mybatis.S_cateDTO">
   		SELECT S_CATEGORY_CODE, S_CATEGORY_NAME
		FROM TBL_S_CATEGORY
		WHERE L_CATEGORY_CODE = #{l_category_code}
   </select>
   
   
</mapper>